-- ============================================================
-- Migration 20260320044335 : PLACEHOLDER
--
-- Cette version a été appliquée en base distante via MCP Supabase
-- pendant la phase initiale. Son contenu (indexes + triggers
-- d'audit + update_updated_at) est entièrement inclus dans
-- 20260320044304_initial_schema.sql côté repo local.
--
-- Le fichier est conservé pour préserver l'historique aligné
-- entre supabase_migrations.schema_migrations (remote) et le
-- répertoire supabase/migrations/ (local) — sinon `supabase db
-- push` refuse d'appliquer toute nouvelle migration.
-- ============================================================

-- no-op
SELECT 1;
